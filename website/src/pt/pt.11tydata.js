// Directory data for the Portuguese tree. Every page under src/pt/ resolves
// its locale, its translations and its URL prefix from this one key, so no page
// front matter has to repeat the language.

export default { lang: "pt" };
