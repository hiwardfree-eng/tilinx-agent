/**
 * The team view's Files section. One agent's real tree at a time (never a
 * merged filesystem), over the shared per-agent Files wiring
 * (`components/agent/agent-files/`).
 */
export { TeamFiles } from "./team-files";
