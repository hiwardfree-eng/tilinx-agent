# All direct dependencies are pinned to exact versions (HOU-671): upgrades are
# deliberate PRs, never a side effect of a reinstall. save-exact keeps future
# `pnpm add` from reintroducing ^/~ ranges.
save-exact=true
