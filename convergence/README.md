# Single-engine convergence — program plan

One engine for local (desktop) + cloud (multi-tenant SaaS). Zero drift.

> **Status: DONE — one host, not two engines.** The convergence is complete: the
> TilinX host (`packages/host` + the pi runtime in `packages/runtime`, protocol
> v3) is the single, default engine for desktop AND cloud, and the legacy Rust
> `engine/` has been **deleted** (record: `convergence/final-cutover.md`). The
> phase history below is kept for context; where it describes the Rust engine as
> the "current default" or a coexisting/rollback build, or the host behind a flag,
> read that as historical — that step has been executed.

**Architecture: one host, not two addons.** Not "shared library + local supervisor + cloud control plane" — that still lets handlers drift. Instead: ONE deployment-agnostic server (the **TilinX host**, `packages/host`) with ONE router, ONE `authorize()` seam, ONE domain layer — and two **adapter profiles** wired in `main()`:

| Port | Local adapter | Cloud adapter |
|---|---|---|
| RuntimeLauncher | subprocess spawn/sleep per agent (server mode) | CloudRun per-turn / GKE pods (exists) |
| Vfs (agent files) | real FS | GCS prefix (exists: `turn/files.ts`) |
| Identity | single-user, always-owner, offline | Supabase JWT (exists) |
| Events | FS watcher → TilinXEvent | post-turn-sync synthetic events |
| Store (tenancy index) | FS scan of `~/.tilinx/workspaces` | Postgres (exists) |
| Bus | in-process | Redis (exists) |
| Scheduler | in-process cron loop | leader-elected via bus lock |
| Credentials | host-owned connect-once, `/sandbox/credential` | same code (exists) |

Drift prevention: domain logic exists once in the host; adapters pass shared **contract-test suites**; the automated cross-profile guard is `packages/host/src/dual-profile.test.ts` (boots BOTH profiles over real HTTP and asserts byte-identical v3 behavior) layered on the per-port contract suites — the live, real-provider cross-profile run remains the MANUAL `convergence/parity-checklist.md` Layer 3 until a non-interactive test-provider credential exists (no Playwright); `/v1/capabilities` replaces "am I web/desktop" branches. These checks now run in CI on every PR: `.github/workflows/ci.yml` (`on: pull_request`) installs the pnpm workspace and runs Biome (`pnpm check`), workspace typechecks, and Vitest suites — previously they ran only locally. The open/closed repo split (open local stack "TilinX" vs closed cloud adapters "TilinX Cloud") is documented in `BOUNDARY.md` (repo root) and machine-enforced by `scripts/check-boundaries.mjs` (`pnpm check:boundaries`, wired into the CI gate): open packages (`protocol`/`domain`/`runtime`/`ui`) may never import a cloud adapter, and inside the host only `main.ts`/`admin/**` may touch a concrete cloud adapter — everything else goes through ports. The closed `@tilinx/host-cloud` package (cloud adapters Pg/Gcs/Gke/Redis + operator-admin + the cloud `main.ts`) has since been RETIRED and deleted — the shipped cloud is the private gateway + per-agent engine pods running the open host/runtime (see the multiplayer/gateway sections below), so the multi-tenant host profile was an architecture we moved past; the boundary rules stay locked here so the open repo can never grow cloud-lib imports.

**Base runtime** (`packages/runtime`) stays single-workspace/single-credential/tenancy-free forever. Gains only: skills loading (pi-native), context files, conversation rename/delete, title summarize.

**Protocol v3** = v2 conversation core verbatim, nested under `/v1/agents/:id/conversations/*`, + domain families (workspaces, agents, files, skills, routines, activities, config, providers, preferences, portable, store) + global `/v1/events` channel carrying the existing TilinXEvent vocabulary (keeps `use-agent-invalidation.ts`). Types in `packages/protocol`. Frontend talks ONLY to the host, every deployment. `@tilinx-ai/engine-client` keeps its function surface, transport rewritten. `packages/web/src/engine-adapter/` (the v1-faking synthesizer) gets deleted.

**Resumable conversation streams** (additive to v3): every conversation SSE frame carries a per-conversation monotonic `seq` (envelope + SSE `id:` line), and every turn-scoped frame a server-minted `turnId` — also persisted on the turn's user+assistant `ChatMessage`s and carried in `sync.data.turnId` for the running turn, so a client resyncing across a turn boundary can match history to a live turn and never splices another writer's turn (teammate, second tab, routine) into its own. The events route takes a resume cursor (`?after=` / `Last-Event-ID`) and replays missed in-flight frames gap-free from a bounded per-turn replay buffer, falling back to a `sync {resync: true}` that tells the client to refetch history. Contract + types in `packages/protocol/src/wire.ts`. The shared implementation is `packages/runtime-client`: `replay.ts` (ReplayLog — each stream's ONE sequencing authority; upstream seqs are never adopted), `stream-channel.ts` (StreamChannel — the append→reduce→fan-out→clear-on-terminal publish ordering, used by the runtime bus AND the host's turn relay), and `stitch.ts` (`serveResumableStream` — the subscribe-first connect/replay/resync stitch shared by both events routes; the seq dedupe covers only the initial flush, so a seq regression — e.g. an evicted/deleted conversation — never starves a live subscriber). Relay hardening: the bus dialect is versioned (`turn:ev2:`/`turn:snap2:`, `packages/host/src/turn/relay-dialect.ts`) so a mixed-version fleet never cross-feeds payload shapes; persisted snapshots parse defensively (garbage → resync); and a dead-pump reaper at subscribe time (`TurnRelay.reapIfDead`) terminates a turn whose snapshot says running while no replica holds the agent's lease — clients get a synthesized terminal `error` carrying the dead turn's `turnId` instead of a spinner stuck until the snapshot's TTL. Turns already ran server-side regardless of subscribers — this makes observing them survive disconnects (mobile, backgrounding, network switches) without truncation. The client side of this — how the SDK, runtime-client wire layer, tokens, and component inventory keep all three surfaces in step — is the maintenance contract in `knowledge-base/client-architecture.md`.

## Sub-docs (this directory)

- `final-cutover.md` — the record of the completed final step: the Rust `engine/` deleted + the host made the default desktop build. (DONE; kept as the what-happened + rollback note.)
- `parity-checklist.md` — the dual-profile parity gate: the automated CI layer (`packages/host/src/dual-profile.test.ts` + per-port contract suites) plus the manual, real-provider rows a human runs once on the packaged app.
- `migration-gate.md` — the chat-history + provider/model migration, VERIFIED against a copy of real Rust-era `~/.tilinx` (38 conversations, 7 agents, 0 legacy files touched, downgrade-safe).
- `packaged-app-launch.md` — the one manual step: launch the notarized packaged `.app`, connect a real provider, run one real turn against the bun-compiled host sidecar.
- `follow-ups.md` — non-blocking clean-ups (the `/agents/*`→`/v1/agents/*` prefix, v3 client consolidation, `createControlPlaneServer`→`createHostServer` rename, deploy-identity renames).
- `migration-plan.md` / `phase0-findings.md` — the original plan + P0 spike results.

## Phases

- **P0 — Spikes + decisions.** ✅ done — see `phase0-findings.md`.
- **P1 — Protocol v3 + runtime additions.** ✅ done (#21, #22). `packages/protocol`; runtime: rename/delete, skills dir, context files, summarize.
- **P2 — control-plane → host.** ✅ ports done: RuntimeChannel (ProxyChannel/TurnChannel — the server never branches on hosting model), RuntimeLauncher (was SandboxManager), Vfs (was ObjectFiles; Memory/Gcs/**Fs** adapters + shared contract suite), SingleUserVerifier, /v1/version + /v1/capabilities live on the cloud host. The `control-plane` → `host` DIRECTORY rename (`packages/control-plane` → `packages/host`, package `@tilinx/host`, including the native build paths + cloud deploy scripts/k8s build-context refs) is now DONE.
- **P3 — Domain in TS.** 🟡 in progress. Done: `packages/domain` (TextStore port — the host Vfs satisfies it structurally; `.tilinx` layout + schema seeding from `ui/agent-schemas`; activities/routines/runs/config/learnings normalize+CRUD as pure fns, junk → diagnostics, parse errors throw with the key named) + host routes `/agents/:id/{activities,routines,routine_runs,config,learnings}` over `deps.vfs`, schemas seeded on agent create — live on cloud first. Also done: skills family — SKILL.md frontmatter parse (YAML 1.1 `featured: yes` normalized), list/read/create/edit/delete at `/agents/:id/skills[/:slug]`; created skills reach the agent next session with no extra plumbing (pi loads the same dir).

Also done: global `/v1/events` SSE channel — `EventHub` over the publish/subscribe subset of the TurnBus (Redis multi-replica in cloud, in-process otherwise; one bus now backs both turns and events). Per-user channel (`events:<userId>`) so cloud tenants never cross-leak; the host emits `ActivityChanged`/`RoutinesChanged`/`ConfigChanged`/`LearningsChanged`/`SkillsChanged`/`AgentsChanged` after every successful mutation, carrying the same vocabulary the Rust firehose used (so `use-agent-invalidation.ts` survives the P6 engine-client rewrite). Locally (P4) the FS watcher feeds the same hub.

Also done: scheduling ENGINE — `@tilinx/domain` cron eval (`croner`, timezone-aware: `validateSchedule`/`nextRun`/`dueAt`/`routineConversationId`/`createRoutineRun`) + the host `Scheduler` driver (scan all agents each tick, fire routines due in the `(lastTick, now]` window exactly once, record runs, mark errored runs instead of leaving them stuck "running"). Multi-replica safe with NO leader election: the due instant is the replica-independent cron time, so a per-`(routine, instant)` `setNx` lock arbitrates. Firing is a `RoutineFirer` port (injected; cloud vs local differ). Also wired `validateSchedule` into routine create/update — a bad cron now 400s instead of saving a routine that silently never fires.

Also done (**3.4b**): the Scheduler is wired into `main` with a REAL firer. New `RuntimeChannel.fireTurn(ctx, conversationId, text)` on both channels — `ProxyChannel` POSTs the prompt to the standing runtime's conversation endpoint; `TurnChannel` calls the extracted `dispatchTurn` core (a behavior-preserving split of `startTurn` — the HTTP route and the firer now share one path, and the rich quota message is preserved). `ChannelRoutineFirer` routes a due routine through the same channel a user message uses. Routines now actually fire on schedule in cloud.

Also done (**3.4c**): routine run completion, exact parity with `engine/.../routines/runner.rs`. Domain: `routinePrompt` (appends the verbatim `SUPPRESSION_INSTRUCTION` when `suppress_when_silent`), `responseIsSilent` (`ROUTINE_OK` token, trimmed, starts/ends, case-sensitive), `extractRunSummary` (token stripped, 200-char `…` cap, "Nothing to report"), `completeRoutineRun`, `routineActivity`. Host `reconcileAgentRuns` (called each tick): reads each running run's conversation off the vfs, classifies silent/surfaced from the agent's reply, a surfaced run gets a `needs_you` board Activity (reused by `session_key` across runs), a reply-less run past 15 min is errored. Multi-replica safe (per-run `setNx`). The firer now sends `routinePrompt(routine)`.

Also done: workspaces + preferences families. `@tilinx/domain` preferences = a per-workspace key-value doc at `ws/<wsId>/preferences.json` (above the agent prefixes, survives agent deletion; corrupt doc → empty, never crashes boot gates). Host: `GET /v1/workspaces` (the caller's personal workspace, wire-mapped — no slug/runtime leak), `PATCH /v1/workspaces/:id` (locale, owner-only), `GET/PUT /v1/preferences/:key` (per-user). Note: new routes use the target `/v1/*` shape; the legacy `/agents/*` (un-prefixed) routes move to `/v1/agents/*` in P6 (one client-coordinated rename).

Also done: portable agents — `@tilinx/domain` `packAgent`/`unpackAgent`/`portableInventory` (the `.tilinxagent` zip: manifest + CLAUDE.md + skills + routines + learnings, fflate, future-format rejection, malformed-entry drop) + host routes `POST /agents/:id/portable/export`, `POST /v1/portable/{preview,install}`. The same format moves an agent between desktop and cloud unchanged. The v1 LLM-anonymization + security-scan are deferred (heuristic enhancements, not the round-trip core).

**P3 is complete.** The migration-chain is P5 tooling.
- **P4 — Local profile.** ✅ complete. `ProcessLauncher` (subprocess `RuntimeLauncher`; `RuntimeProcessSpawner` prod path) + the EXISTING `ProxyChannel` → local dispatch is the same channel code as cloud. `FsWatcher` + pure `classifyChange` feeds the same `EventHub`. `WorkspacePaths` seam (CloudPaths/LocalPaths) keeps the handlers layout-blind. `LocalWorkspaceStore` (the desktop tree IS the store; agent.id = `<W>/<A>`; URL encode/decode for the slash) + `listWorkspacesForUser`. `FileCredentialStore` + connect-once env threaded through the launcher (keyless local runtimes fetch from the host's `/sandbox/credential` — Gate #2 locally). **`buildLocalHost`** wires every local adapter into the SAME `createControlPlaneServer` + FsWatcher + Scheduler; **`local/main.ts`** is the desktop sidecar entry (emits the `TILINX_HOST_LISTENING` banner the Tauri shell parses). `WorkspaceRuntime` gained `local`. The `packages/control-plane` → `packages/host` dir rename (with native build + cloud deploy/k8s refs) is DONE, and the cloud adapters (Pg/Gcs/Gke/Redis + operator-admin + cloud `main.ts`) were extracted into the closed `@tilinx/host-cloud` — since RETIRED and deleted (the gateway + per-agent engine pods won as the cloud architecture) — with the open/closed boundary still locked by `BOUNDARY.md` + `scripts/check-boundaries.mjs`. Host-as-sidecar packaging (Bun compile + Tauri externalBin) is DONE (originally behind the `host-sidecar` cargo feature, since removed — spawning the host is now the default/only path). The one remaining item is the live end-to-end connect on the packaged app (needs a real provider) — folded into the final-cutover gate (`convergence/final-cutover.md`, `convergence/packaged-app-launch.md`).
- **P5 — Desktop cutover + migration.** 🟢 functionally complete; the only thing left is the gated final cutover (`convergence/final-cutover.md`). **Flag-gated** so `main`'s default build stays the Rust engine (releasable + instant rollback) until the host path is proven, then flip the default. Key realization: `packages/web` already aliases `@tilinx-ai/engine-client` → the QA'd v3 adapter, so the cutover is *reuse*, not an engine-client rewrite. Slice 1 done: `app/` conditionally aliases `@tilinx-ai/engine-client` → the adapter and `engine.ts` points at the host (control-plane mode) when `VITE_NEW_ENGINE_URL` is set; default path untouched (app typecheck green, flagged frontend build green). Ordering: (1) frontend↔host **[done]** (incl. `pnpm tauri dev` runs without the Rust engine via host_mode in lib.rs + build.rs placeholder; fetch-SSE reactivity — EventSource is dead in the Tauri webview). (2) close gaps — **DONE**: removed mobile/tunnel + worktrees UI (left vestigial `worktree_path`), built attachments upload (`POST/DELETE /agents/:id/attachments` → `<agentRoot>/.attachments/<scope>/`), conversation title (`POST /title` on runtime + adapter), run-routine-now (`POST /agents/:id/routines/:id/run`, shared `fireRoutineRun`). Cut-UI sweep **DONE** too: removed composio, store/marketplace/community/github-import, claude-CLI install, gemini (114 files / −5390); single workspace is already the only one. (3) Bun-compiled host **sidecar DONE** (#47): `host-sidecar` cargo feature (default = Rust engine, untouched), `scripts/build-host-sidecar.sh` (62MB binary, boots + serves), build.rs stages it + lib.rs/supervisor spawn it + parse `TILINX_HOST_LISTENING`. The host-sidecar release CI is a committed workflow (`.github/workflows/host-sidecar-release.yml`, mac universal signed+notarized / Windows MSI x64+arm64 / Linux AppImage+deb, `host-v*`/dispatch-gated, Rust `release.yml` untouched). The PR CI gate (`.github/workflows/ci.yml`, `on: pull_request`) is also committed. Remaining sidecar bit: the packaged-`.app` live run (user — see `convergence/packaged-app-launch.md`). (4) **migration chain + capture real Rust-era data + verify downgrade — MUST finish before P6 (deletes the oracle)**. The chat-history migration is IMPLEMENTED + wired: `src/migrate/{chat-history,reconstruct,linkage}.ts` (transcripts → v3 conversations + a synthesized pi session from user/assistant text pairs, idempotent `.migrated` marker, source read-only), tested (`chat-history.test.ts`), and run on boot from `local/host.ts` with the db path defaulted in `local/main.ts` (`TILINX_CHAT_HISTORY_DB`). Migration gaps now CLOSED: (a) `@tilinx/domain` `migrateProviderModel` remaps legacy provider/model ids to the pi catalog (`openai`→`openai-codex`, validate-or-fallback to provider default, never auto-upgrade, diagnostic on unknown) applied read-time (web config→settings bridge + runtime `safeGetModel`), so the user's file is never mutated → downgrade-safe; (b) the one-time "Reconnect your AI" post-migration screen exists (`migration-reconnect-screen.tsx`, gated on the `/v1/version` `chatHistoryMigrated` flag, reuses `ProviderPicker`); (c) the gate was VERIFIED on a copy of real Rust-era `~/.tilinx` — 38 conversations across 7 agents migrate cleanly, transcripts valid, 38 resumable pi sessions, idempotent re-run, db read-only, **0 legacy files touched (downgrade-safe)**, 7 orphans honestly logged. See `convergence/migration-gate.md`. (5) parity net = the P6 gate — **automated layers DONE**: the per-port contract suites (#49) + an assembled **dual-profile parity test** (`src/dual-profile.test.ts`: boots both profiles over HTTP, asserts byte-identical v3 behavior on the no-drift surface + the documented asymmetries). The full gate (incl. the live, real-provider Layer-3 checklist that needs the running apps) is spec'd in `convergence/parity-checklist.md`. (6) P6 deletion only after the gate is green. Loose ends: the onboarding "try" tutorial prompt still describes a Composio demo (`app/src/components/onboarding/tutorial-system-prompt.ts`) — product copy rewrite, not broken code. Tauri spawns host sidecar (same supervisor contract). Migration: idempotent, versioned, copy-never-move. Reconnect-onboarding card. Downgrade always works.
- **P6 — Consumer sweep + deletion.** 🟡 in progress. **Web cutover merged (#36, #37):** `packages/web/src/engine-adapter/` now talks to the local-profile host for real — un-faked activities/routines/skills/board/config/learnings (`readAgentFile`→host agentfile route), wired routine/skill mutations, `/v1/events` reactivity, `VITE_CP_DEV_TOKEN` dev-auth bypass, the ported product system prompt (`packages/host/src/tilinx-prompt.ts`), supervisor hardening, and QA-found fixes (PUT missing from CORS; provider status per-agent; Bun SSE buffering → per-frame flush; **chat turn completion wrote the board status to localStorage while the board reads the host → the card hung in "running" forever; now the turn's status write goes through a cloud-aware setter that PATCHes the host**). Validated live by the user (chat/skills/board/routines/activities/events/config/learnings, no fakes). Files tab now served by the HOST (`handleFiles` over the workspace vfs, intercepted in `routes/agents.ts` before the channel — shared cloud+local, one path, internals `.tilinx`/`.agents` hidden + refused; the redundant `dispatchCloudrun` copy was removed). Composio is NOT cut — it's re-wired on the host (in-process REST behind the `IntegrationProvider` port, `src/integrations/` + `/v1/integrations` + `/sandbox/integrations` routes + the `integration_search`/`integration_execute` runtime tools) AND in the web adapter (`integrationStatus`/`integrationToolkits`/`integrationConnections` in `engine-adapter/client.ts`). The store surface was resolved in HOU-662: `listInstalledConfigs()` is REAL again and GitHub agent install exists at the API level (host `GET /v1/agent-configs` + `POST /v1/agents/install-from-github` over the `agentConfigs` library dep — the legacy `~/.tilinx/agents` tree locally, `users/<userId>/agent-configs` on the object store in cloud), though the install has NO UI affordance — matching the shipped Rust desktop, which also hides it. The store catalog, `checkAgentUpdates`, and workspace-from-GitHub stay cut and their dead client paths are removed. Cut features are now GONE, not pending conversion: `mobile/`, `tilinx-relay/`, `examples/smartbooks/`, and `always-on/` (the legacy Rust-engine VPS image) are deleted, along with `.github/workflows/docker-publish.yml`. Still ahead — all gated on the live parity gate, tracked in `convergence/final-cutover.md`: delete the engine-adapter + `engine/` + v1 client + CLI bundling, flip the default to the host, and the `/agents/*`→`/v1/agents/*` prefix (a coordinated host+client rename, `convergence/follow-ups.md` #1). Update all KBs. (`control-plane`→`host` dir rename DONE; the closed `@tilinx/host-cloud` extraction has since been superseded by its full retirement — deleted, survives in git history.)

Critical path: P0 → P1 → P3 → P5. P1∥P2, P3∥P4.

## Migration (existing local users)

| Data | Action |
|---|---|
| Workspace/agent tree + `.tilinx/*` JSON | unchanged (schemas already from `ui/agent-schemas`) |
| `config.json` model IDs | CLI IDs → pi IDs, mapping table, never auto-upgrade |
| History (`chat_feed` + `.tilinx/sessions`) | transcripts → v3 conversations (always visible) + synthesized pi session from plain user/assistant pairs (agent remembers; tool/thinking fidelity impossible — say so). Mechanism proven: `packages/runtime/src/session/resume.test.ts` |
| Credentials | NOT migratable (different OAuth clients). One-time "Reconnect your AI" card |
| Skills | none needed — TilinX SKILL.md = pi native format |
| CLAUDE.md | none needed — pi discovers CLAUDE.md natively |
| Preferences / engine tokens | prefs → host store; legacy engine tokens regenerate (tunnel/mobile were cut, nothing to re-pair) |

## Self-host (single-user VPS)

The local profile IS a server (`buildLocalHost` = `createControlPlaneServer` +
local adapters), so self-hosting is a deployment of it, not new code: bind
`0.0.0.0` (`TILINX_HOST_BIND`, default loopback so the desktop stays
unexposed), persist `~/.tilinx` to a volume, put TLS in front. One container
runs the host, which spawns pi runtimes in-container over loopback exactly like
the desktop — "local = the cloud shrunk to one machine," literally the same
code. Artifacts + guide: `selfhost/` (Dockerfile, Caddy TLS compose, README).
Replaced the legacy `always-on/` Rust-engine VPS image (now deleted). The host boot
was verified on a clean-room per-package install; the image build itself wants a Docker host.

## Managed hosted pods

TilinX's managed cloud reuses the self-host shape, not a new runtime. The public repo
builds `selfhost/Dockerfile --target engine-pod` (manually publishable via
`.github/workflows/ts-engine-image.yml` to the multi-arch
`ghcr.io/gettilinx/tilinx-engine-pod` image): one open host process that
spawns one pi runtime per agent over loopback, with `/data` as `TILINX_HOME`,
`TILINX_MANAGED_CLOUD=1`, and `TILINX_CODE_EXECUTION=local` (HOU-669: the
agent's bash runs in-container — the pod is single-tenant and network-policied,
so the container is the sandbox, same as self-host; the image ships
curl/wget/git/python3 for it). Capabilities
advertise cloud profile, no OS reveal/terminal, `local-bash` code execution,
the full desktop provider set — including the OpenAI-compatible BYO endpoint
(`openaiCompatible`), now cloud-enabled: a pod accepts a **public HTTPS endpoint**
the user hosts (a tunnel or a directly hosted server), and the save route
validates the base URL against the pod's public-`:443`-only egress (rejecting
plain http, a custom port, and private/loopback/link-local hosts) — and
integration `composio`. The private repo supplies the
Supabase-authenticated gateway, maps user `sub` to the pod service, replaces the
user bearer with `TILINX_HOST_TOKEN`, and owns Kubernetes Deployment/PVC/Secret/
Service/NetworkPolicy creation. Desktop hosted mode uses `VITE_HOSTED_ENGINE_URL`
and the existing Supabase session token; `VITE_NEW_ENGINE_URL` static-token mode
stays for dev/self-host. The hosted Google-login gate is toggled by
`VITE_HOSTED_ENGINE_AUTH` (`oauth` by default when a hosted URL is set; `static`
points at the gateway with a static bearer for service-token smoke tests) — see
`knowledge-base/auth.md` for the table + the local-kind login recipe.

## Host-sidecar release CI

**Updated (HOU-628): the desktop release now builds the TS engine directly.**
`.github/workflows/release.yml` (tag `v*`) was rewritten to bun-compile the host
sidecar and build Tauri with `--features host-sidecar` + `VITE_NEW_ENGINE=1`,
replacing the Rust `tilinx-engine` build + the codex/composio/git-bash CLI
staging. `.github/workflows/engine-release.yml` (tag `engine-v*`) likewise now
bun-compiles the **standalone** host binary (`tilinx-host-<triple>`) instead of
the Rust engine. (The earlier plan kept `release.yml` on Rust and put the TS build
in a separate `host-sidecar-release.yml` / `ts-engine-desktop-artifacts.yml`;
those were never committed — the build logic they described now lives directly in
`release.yml` + `engine-release.yml`.)

**Cloud desktop channel (tag `cloud-v*`).** `release.yml` now drives TWO channels
from one pipeline: `v*` = the LOCAL build (host sidecar / connection chooser,
unchanged) and `cloud-v*` = a CLOUD build that bakes the managed gateway URL in
from the `HOSTED_ENGINE_URL` **secret** (never a literal) as
`VITE_HOSTED_ENGINE_URL` + `VITE_HOSTED_ENGINE_AUTH=oauth`, so `resolveEngine` →
`hosted-oauth` and the user just signs in with Google — no URL to enter. Every
cloud-specific line is gated on `startsWith(github.ref_name, 'cloud-')`, so a `v*`
build is byte-identical to before. The cloud channel repoints the in-app updater
at a distinct `latest-cloud.json` manifest (`scripts/ci/point-updater-at-cloud-manifest.sh`,
Node-based so it runs on windows-11-arm too) so a cloud app can never auto-update
into a local build (dropping the baked gateway) or vice-versa. Cloud releases are
also created as **prereleases** so they can never become GitHub's "latest release"
(which is the newest NON-prerelease) and shadow the local channel's baked
`…/releases/latest/download/latest.json` endpoint — otherwise the first published
cloud release would 404 every installed local app's auto-updater. Because the
`/latest/` alias only ever resolves to non-prereleases, the cloud app's updater is
instead pinned to a FIXED tag: `…/releases/download/cloud-latest/latest-cloud.json`.
The rolling `cloud-latest` pointer release (itself a prerelease, so it can't shadow
the local channel either) is maintained by
`.github/workflows/cloud-updater-manifest.yml`, which copies the finalized
`latest-cloud.json` onto it whenever a `cloud-v*` release is published — so cloud
auto-update only ever advertises published (post-QA) builds, and re-publishing an
older cloud release doubles as channel rollback (HOU-667). The packaged cloud app
also skips spawning the idle host sidecar: `lib.rs` `host_mode` checks
`option_env!` (compile-time, catches the CI-baked gateway URL) in addition to the
runtime env (dev). The desktop hosted path itself is unchanged app code
(`app/src/lib/engine-mode.ts`, `knowledge-base/auth.md`).

Originally this was a CI-only cutover that flipped only what the RELEASE shipped,
leaving the app's build DEFAULTS on the Rust engine as an instant-rollback oracle.
That final flip has since been executed: a plain `pnpm tauri build` now builds the
host app, the `host-sidecar` cargo feature is gone (no `--features host-sidecar`),
and `engine/` has been deleted (`convergence/final-cutover.md`). A manual-only
QA-artifacts workflow — the
`.github/actions/build-ts-engine-desktop-artifact` composite action + `scripts/ci/`
prepare helpers already exist for it — can upload GitHub Actions artifacts without
cutting a release; it is not wired into a top-level workflow yet.

The platform jobs all bun-compile the host via the one parameterized
`scripts/build-host-sidecar.sh <triple>` (no second compile path to drift):

- **macOS** — universal (arm64 + x86_64, lipo'd from two per-arch bun-compiles),
  signed + notarized + stapled with the SAME Apple secrets + steps as before (DMG
  re-sign, notarize-with-retry, full signing-chain verification, sidecar
  Developer-ID + hardened-runtime asserts), uploaded to the draft release.
- **Windows** — MSI per arch (x86_64 + aarch64 matrix), updater-key signed (`.sig`
  emitted), uploaded. None of the old codex/composio/git-bash CLI staging: pi runs
  providers in-process, so the host carries no CLIs.
- **Linux** — NEW: the CLI-free host makes a Linux DESKTOP build viable for the
  first time. x86_64 AppImage via tauri-action's `--bundles appimage` (passed on
  the CLI, so `tauri.conf.json`'s app/dmg/msi targets stay untouched), uploaded to
  the draft (UNSIGNED, download-only — not in `latest.json`; `.deb` intentionally
  not built). The job also boots the bare host binary (`--verify` → curls
  `/v1/capabilities`), which is the same binary the `selfhost/` server runs.

`build.rs::stage_host_sidecar` stages the compiled host at the
`binaries/tilinx-engine-<triple>` externalBin name (kept on purpose from the
former Rust engine, so `tauri.conf.json` needs no change). The `host-sidecar`
cargo feature that once gated this is gone — staging the host is now
unconditional. End-to-end packaged-`.app` run verification is the one manual step — see
`convergence/packaged-app-launch.md`. The Docker `selfhost/` image is a separate
path — it runs the host from Node/pnpm source, not this compiled sidecar.

## Standing decisions

- API-key providers — **OpenCode Zen + Go, OpenRouter, DeepSeek, Google Gemini, Amazon Bedrock, and MiniMax global** (reversing the earlier "Gemini dropped"). pi ships `opencode`/`opencode-go`/`openrouter`/`deepseek`/`google`/`amazon-bedrock`/`minimax` as built-in in-process providers (OpenAI-compatible gateways + native DeepSeek + native Google + Bedrock Converse Stream + MiniMax Anthropic-compatible global API; intentionally not `minimax-cn`), so no provider engine is needed — only an API-key credential path threaded through the stack: pi `AuthStorage` `{type:"api_key"}` creds; the host `WorkspaceCredential` gains `kind` (api-key = no refresh, `expiresAt:0` sentinel — no Postgres migration, Pg derives `kind` on read); `refreshCredential`/`isExpiring` no-op for api-key; `POST /agents/:id/credential/api-key` + `RuntimeChannel.saveApiKeyCredential` (ProxyChannel stores centrally AND pushes to the standing runtime for instant status; TurnChannel stores centrally, served per-turn); the per-turn `syncServedCredential` now syncs ALL providers. Connect = paste a key in `ProviderApiKeyDialog`, with a one-click "Get your API key" button opening the provider's key page (OpenCode `opencode.ai/auth`, OpenRouter `settings/keys`, DeepSeek `platform.deepseek.com/api_keys`, AI Studio `apikey`, Bedrock console `api-keys`, MiniMax `platform.minimax.io`). Bedrock's pasted key is a Bedrock bearer token; `packages/runtime/src/ai/bedrock.ts` maps pi-coding-agent's generic `apiKey` option to pi-ai's provider-specific `bearerToken` option before each request. **New-engine only**: the catalog gates these behind `newEngineActive()` (the adapter sets `window.__TILINX_NEW_ENGINE__`), so the legacy Rust build never shows them. Reasoning effort flows via the settings bridge (`configWriteToSettings` → `setSettings` → `activeEffort`), with per-model `effortLevels` in the catalog (DeepSeek direct reaches `xhigh`; Gemini caps at `high`; MiniMax caps at `high`; OpenRouter Claude/DeepSeek reach `xhigh`; Bedrock Claude reaches `xhigh`). OpenRouter/DeepSeek/Gemini/Bedrock/MiniMax stay local-only until cloud egress is allowlisted. **OpenRouter OAuth-PKCE** (the no-paste path) is a deferred follow-on (needs an https callback on :443/:3000, web-profile-only).
- Composio: **kept, re-wired, PLATFORM mode** (reversed twice: first the "cut", then the "Composio for you" per-user-account model). pi has no MCP, but its one extension point — custom tools — is enough: a generic `integration_search`/`integration_execute` pi tool calls Composio's hosted v3 REST API in-process. **No bundled CLI**, **no SDK** (raw fetch), **no per-user Composio account**: TilinX holds ONE Composio project key (`x-api-key`, env `COMPOSIO_API_KEY` — never a literal, never in a client binary) and each TilinX user is a plain Composio `user_id`. Users never see Composio — they only OAuth the app itself (connect = `POST /api/v3.1/connected_accounts/link` → real `redirect_url`; the legacy initiate endpoint is retired for managed OAuth as of 2026-07). Auth configs are get-or-created per toolkit on Composio-managed auth (no per-toolkit dashboard step; switching to TilinX's own Google OAuth app later is dashboard-side only). Key custody per profile: **cloud host** = direct adapter with the env key, `user_id` = verified Supabase `sub`; **desktop** = `RemoteIntegrationProvider` gateway that forwards every call to the cloud host's `/v1/integrations/*` with the user's Supabase session (frontend keeps it fresh via `PUT /v1/integrations/session`; `TILINX_INTEGRATIONS_URL` points the sidecar at the gateway), so connections follow the user across desktop/cloud and no key ever ships; **self-host/managed pod** = the operator's own `COMPOSIO_API_KEY`, direct. There is NO per-user integration credential store anymore (the Pg table + `integrations.json` stores are deleted; a leftover `integrations.json` only triggers the one-time "reconnect your apps" security-upgrade notice). Behind the `IntegrationProvider` **port** (`packages/host/src/integrations/`) so a second provider slots in later. **All deployments** (capability flag reflects what's actually configured, not a fork → no drift). Execution stays proxied (`/sandbox/integrations`) — the agent runtime never holds any integration secret. Remaining loose end: the dead onboarding email-mission copy (`tutorial-system-prompt.ts`) still describes the legacy `composio` CLI demo — the phase-(d) repoint of the "try" demo is still open.
- Multiplayer (paid cloud) — **orgs, 3 roles, per-agent assignment, per-(user, agent) integration grants, acting-as identity**. Contracts: `convergence/contracts/C1..C5` (mirrored in the private gateway repo, which ENFORCES all of it — the open repo only carries the plumbing + UI). Roles: `owner` / `admin` (creates agents, assigns agents they have) / `user` (uses assigned agents only). The gateway (private `cloud` repo) owns the Composio platform key, serves `/v1/integrations/*` itself, and is the sole policy enforcer: per-(user, agent) toolkit **grants** filter `integration_search` and 403 ungranted `integration_execute`; the **acting user** is whoever is driving the turn — the gateway mints a per-dispatch `x-tilinx-acting-as` HMAC token, the runtime holds it per-turn (AsyncLocalStorage) and echoes it on `/sandbox/integrations/*`, so John's turns act as John and Sarah's as Sarah on the same agent; routines act as their stored `created_by` via the pod-token path. Open-repo pieces (all optional-field/no-drift: single-player is byte-identical): `ActingContext` on the `IntegrationProvider` port + `RemoteIntegrationProvider` auth modes, routine `created_by`, `ChatMessage.author` + `[From: name]` model framing only in multi-author threads, `Capabilities.multiplayer`/`role` gating ALL org UI (members management, assignment toggles, create-agent hidden for `user`, grant switches in the per-agent Integrations tab). OSS/self-host never sees any of it.
- GitHub Copilot — **subscription OAuth provider** (`github-copilot`), new-engine + LOCAL only. pi-ai already ships it built-in (KnownProvider + the `githubCopilotOAuthProvider` in its OAuth registry), so adding it is registry entries only — runtime `PROVIDERS` (`packages/runtime/src/ai/providers.ts`), host catalog (`packages/host/src/providers.ts`), protocol `ProviderId`, the frontend catalog + logo, and the `synthetic.ts` `toNewProvider` id passthrough (same id both sides). The ONE bit of logic: Copilot's pi-ai login is a GitHub **device-code** flow that OPENS with an optional "GitHub Enterprise URL/domain" `onPrompt` *before* the device code — left unanswered it deadlocks, so `login.ts` `autoPromptAnswer(provider, enterpriseDomain?)` answers it programmatically (the company domain for Enterprise, or `""` ⇒ github.com for individual). Curated models proxy Claude/GPT/Gemini under one sub; their context windows are the FIXED gateway-served values (no plan-gated snap-up). Cloud stays off (egress sandbox doesn't allowlist `githubcopilot.com`).
- GitHub Copilot **Enterprise** (GHE) — Copilot a company provides. pi has exactly ONE `github-copilot` provider/credential slot, so Enterprise is NOT a separate provider OR a separate card: the **single** Copilot card's connect opens a small **Personal vs Company** dialog (`provider-copilot-connect-dialog.tsx`, driven by `useCopilotConnect`). Company collects the firm's GitHub domain and threads it as a non-secret **`enterpriseUrl`** through the whole credential path (login `onPrompt` → runtime auth.json `PiCred` → export/capture → host `WorkspaceCredential` → serve → central **refresh**, which then hits `api.<domain>/copilot_internal/v2/token` instead of github.com); pi's `modifyModels` derives the enterprise API base URL from the token's `proxy-ep` automatically. Personal passes no domain (⇒ github.com). Because it's one card + one credential slot, the frontend needs no per-card status disambiguation or enterprise-aware connect poll — the backend never sees the plan choice, only `enterpriseDomain` on login + `enterpriseUrl` on the credential.
- OpenAI-compatible models (`openai-compatible`) — **any OpenAI-compatible server the user runs** (Ollama, vLLM, LM Studio, LiteLLM…), connected by a **base URL + model id** (+ optional key). Available on **every profile**: desktop/self-host point it at the user's own machine (localhost fine); a **managed cloud pod** points it at a **public HTTPS endpoint** the user hosts (a tunnel or a directly hosted server). Unlike the api-key gateways this is NOT a pi KnownProvider, so there's no catalog — the runtime hand-builds a `Model<"openai-completions">` (`packages/runtime/src/ai/openai-compatible.ts`, the same wire format the OpenCode/OpenRouter gateways use) and persists the endpoint to its own `custom-endpoint.json`; the optional key goes in `auth.json` as pi's `api_key` variant — **a placeholder (`tilinx-local`) for keyless servers**, since pi requires *some* key to resolve a request, and pi resolves it by `model.provider` (== the auth-store key). The pieces: a third `ProviderAuthMethod` `"openaiCompatible"`; a `Capabilities.openaiCompatible` flag (now `true` on local, cloud, AND managed-cloud profiles); a `CustomEndpoint` protocol type; `POST /providers/openai-compatible` on the runtime + `POST /agents/:id/provider/openai-compatible` on the host (gated on the capability, and — on the managed cloud profile only, `gatewayFronted` — validated against the pod's public-`:443`-only egress: `src/custom-endpoint-validation.ts` rejects plain http, a non-443 port, and private/loopback/link-local hosts, so an unreachable URL is a clean 400 at save time rather than an opaque per-turn failure; desktop/self-host skip the check and keep accepting localhost); and `RuntimeChannel.saveCustomEndpoint` — ProxyChannel POSTs the live standing runtime, TurnChannel writes `custom-endpoint.json` into the agent's object-storage prefix for the next turn to hydrate. Frontend: a dedicated `OpenAiCompatibleDialog` (base URL + model + optional name/key), gated on the host's `openaiCompatible` capability (`getVisibleProviders` → `showOpenaiCompatible`), so it surfaces on web/hosted and self-host too — wherever the profile reports the capability — not desktop-only; the model is dynamic so the static catalog has none and the chat picker surfaces the engine-reported model (`providerStatus.activeModel` → `pickerModelRows`). **Known follow-up (pre-existing, tracked separately):** on desktop the agent's in-process `bash` inherits `TILINX_RUNTIME_TOKEN`, so a prompt-injected agent could self-call any loopback runtime route (incl. this one) to redirect model egress — scrub secrets from the bash child env / never trust an empty loopback token.
- Anthropic in cloud: the **multi-tenant per-turn Cloud Run image stays off** (ToS + shared per-turn process); the **single-tenant managed engine pod now supports Claude subscriptions** via a zero-terminal desktop→pod credential handoff (detailed below). Local keeps Anthropic + Codex + GitHub Copilot OAuth. Config asymmetry, not code fork. **Per-provider turn execution (PR #636):** turns run through a pluggable `HarnessBackend` seam (`packages/runtime/src/backends/`, `types.ts` + `registry.ts`); both the long-lived server and the per-turn cloud runtime drive turns through it, emitting the same `WireEvent` dialect. **pi is the default backend** (`setDefaultBackend`); `anthropic` registers the Claude Agent SDK backend (`backends/claude/`) so its turns AND titles (`title.ts`, compliance-critical) run the real `claude` subprocess — never pi's in-process Anthropic client, the harness-spoofing subscription-OAuth replay Anthropic server-blocks since 2026-04. Full provider-auth model (setup-token paste connect, Codex loopback topology) is `knowledge-base/auth.md` → "Provider connect + turn execution (TS engine)". **Zero-terminal hosted Claude connect (desktop→pod credential handoff):** on the desktop `claude auth login` mints a Claude subscription OAuth credential (access + refresh + expiry) and caches it locally; a LOCAL engine reads it directly, but a hosted pod can't, so the desktop EXTRACTS it and PUSHES it to the pod through the gateway. Host route `POST /agents/:id/credential/claude-oauth` (`routes/agents.ts`, same owner authz as capture) takes the CLI's `{claudeAiOauth:{accessToken,refreshToken,expiresAt,scopes,subscriptionType?}}` envelope, validated by `parseClaudeOAuthEnvelope` (`packages/protocol`, re-exported via `@tilinx/runtime-client`) — the ONE hard requirement is a non-empty `accessToken`; `refreshToken`/`expiresAt`/`scopes` are optional and type-checked only when present (a real minted credential must never be 4xx'd over a field the SDK, not TilinX, interprets — that would strand every user on the paste fallback). Malformed JSON still 4xxes and the desktop falls back to the paste flow. **Anthropic-in-cloud is per-AGENT, not per-workspace:** the credential materializes on the connected agent's pod and each pod self-refreshes its OWN copy — deliberate, because two pods sharing one refresh token would race on OAuth refresh rotation and invalidate each other. `RuntimeChannel.saveClaudeOAuthCredential` DUAL-writes (mirroring `saveApiKeyCredential`): store the credential centrally (durability + connected marker) AND push it to the standing runtime's `POST /auth/anthropic/oauth-credential`, which materializes it as `<claudeLoginConfigDir()>/.credentials.json` (= `/data/claude-login/.credentials.json` on the PVC), 0600, atomic — the exact file the Claude Agent SDK + `claude auth status` read on Linux — then warms the connected signal (`refreshAnthropicCredential`). The SDK **self-refreshes** from the refresh token ON the pod, so there is NO central Anthropic refresh and the stale-token bug is gone; anthropic is therefore BYPASSED in the per-turn serve loop (`auth/serve.ts` excludes it — pulling a Gate #2 access-only token per turn would fight the SDK's file). **Refresh-token-on-pod is a DELIBERATE, explicitly-gated departure from Gate #2** ("sandbox never holds a refresh token"), scoped to the single-tenant, network-policied managed pod ONLY: `TurnChannel.saveClaudeOAuthCredential` (the multi-tenant per-turn Cloud Run channel) REFUSES the push, so no Anthropic refresh token ever lands in a shared process. Compliance gate unchanged: the credential is minted by Anthropic's real `claude` CLI and consumed by the real Claude Agent SDK subprocess — never pi's in-process Anthropic client. The gateway-proxied surface + PVC materialization + the refresh-token-on-pod scoping are documented in `cloud/INTEGRATION.md`. **Packaging (PR #636 backend, #647 binary):** the SDK spawns a per-platform ~250 MB native `claude` binary (the SDK's own optional dep). It ships ONLY where Anthropic can run: the self-host + managed engine-pod images (`selfhost/Dockerfile` host-base installs it; resolves via `require.resolve` under Node) and the desktop Bun sidecar (`scripts/build-host-sidecar.sh` stages the platform `claude` next to the compiled sidecar in `target/host-sidecar/`; `require.resolve` can't reach it inside `$bunfs`, so `packages/runtime/src/backends/claude/binary-path.ts` points the SDK at the `<dir of sidecar>/claude` sibling via `pathToClaudeCodeExecutable`). The multi-tenant per-turn Cloud Run image (`packages/runtime/Dockerfile`) strips it — Anthropic never runs there. **Desktop externalBin wiring is DONE (Phase A):** `build.rs` (`stage_claude_binary`) stages `claude-<triple>` into `binaries/`, `tauri.conf.json` `externalBin` lists `binaries/claude`, and `release.yml` force-installs both macOS arch SDK packages, lipo's a universal (~470 MB) `claude`, and verifies it is a signed + hardened + dual-slice Mach-O in `Contents/MacOS/` next to `tilinx-engine`. The one remaining manual step is confirming notarization on a real signed universal build (no signing identity in CI-less dev).
- Open source: Option B (open local stack: runtime + domain + host + local adapters + app + ui; cloud adapter profile closed). Apache-2.0 + DCO. Execute AFTER P6, never during. Closed repo owns no domain types, runs open contract tests, stays adapters+infra only.
