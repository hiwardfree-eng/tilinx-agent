# Agent Store Image — build + push to Artifact Registry
#
# The Agent Store frontend (agentstore/) is a Next.js app that talks to the Go
# gateway's /v1/agentstore API. This repo is its single source of truth. This
# workflow ONLY builds and publishes the container image; it never touches the
# GKE cluster. This repo is public, so a compromised third-party Action or
# dependency here must not be able to reach production. The actual roll (update
# the agentstore Deployment to the new digest + restart) happens in the private
# gettilinx/cloud repo (.github/workflows/roll-agentstore.yml there), triggered
# by the repository_dispatch fired below. That handoff is the trust boundary and
# mirrors engine-pod-image.yml → cloud/roll-engine.yml.
#
# Two triggers, two audiences:
#   - push to main (paths-filtered, checked in "Determine whether to build"
#     below): continuous build → dispatches roll-agentstore →
#     cloud/roll-agentstore-staging.yml rolls staging.
#   - push of an agentstore-v* tag (no path filter — the tag means "ship
#     this", and combining paths with a tags trigger is unreliable in GitHub
#     Actions, since a tag push often carries no commit list to diff against):
#     dispatches roll-agentstore-prod → cloud/roll-agentstore.yml rolls prod.
# The tag pattern is deliberately NARROW: this repo's tag namespace already
# carries other release trains — v*/cloud-v* (desktop, release.yml),
# cloud-latest (a MOVING pointer that gets re-pushed), ios-v* (TestFlight),
# engine-v* (self-host host binary), engine-pod-v* (prod engine fleet). Each
# train versions independently; only agentstore-v* may roll prod agentstore.
#
# Publishes two tags per build:
#   agentstore:<git-sha>  — immutable, by-digest reference used for the roll.
#   agentstore:latest     — moving pointer to the newest build (convenience).
#
# ── GCP auth: Workload Identity Federation ────────────────────────────────────
#   SA: github-deploy-engine@gettilinx.iam.gserviceaccount.com — Artifact
#   Registry Writer ONLY (no cluster role), restricted by the WIF provider
#   condition to attribute.repository=='gettilinx/tilinx'. The same SA + WIF
#   provider + registry the engine-pod-image.yml build uses; do NOT reuse the
#   broader github-deploy@ SA (that one also carries Kubernetes Engine Developer).
#
# ── Required GitHub secrets ────────────────────────────────────────────────────
#   CLOUD_DISPATCH_TOKEN — fine-grained PAT scoped ONLY to gettilinx/cloud, with
#   the minimum permission GitHub requires for the repository_dispatch endpoint.
#   Used solely to notify cloud that a new image is ready; carries no GCP access.
#   FIREBASE_API_KEY / FIREBASE_AUTH_DOMAIN / FIREBASE_PROJECT_ID — the Firebase
#   web client config (public identifiers by design), shared with release.yml.

name: Agent Store Image

on:
  push:
    branches: [main]
    tags: ["agentstore-v*"]
  workflow_dispatch:

# One build at a time. cancel-in-progress is false so an in-flight build + push +
# dispatch is never interrupted mid-way — newer runs queue behind it.
concurrency:
  group: agentstore-image
  cancel-in-progress: false

env:
  GCP_PROJECT: gettilinx
  REGION: us-east1
  REGISTRY: us-east1-docker.pkg.dev/gettilinx/tilinx

jobs:
  build:
    name: Build & push agentstore
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      id-token: write # Workload Identity Federation

    steps:
      - uses: actions/checkout@v4

      # An agentstore-v* tag or a manual dispatch always builds (the tag IS
      # the release decision; no other tag pattern reaches this workflow).
      # A push to main only builds when it touched a relevant path
      # — this replaces the old trigger-level `paths:` filter, which can't
      # apply to branch pushes only once the trigger also matches tags.
      #
      # Diffs github.event.before (main's tip BEFORE this push) against the
      # new HEAD, NOT just HEAD^ — a single push can carry several commits
      # (e.g. a non-squash merge), and diffing only the last commit against
      # its immediate parent would miss relevant paths touched by an EARLIER
      # commit in the same push. `before` isn't guaranteed to be present in
      # the shallow clone actions/checkout leaves behind, so it's fetched on
      # demand; if that fetch fails (or before is the all-zeros SHA GitHub
      # sends for a branch's first-ever push), fail open and build anyway —
      # a wasted build is cheap, a silently skipped release isn't.
      - name: Determine whether to build
        id: gate
        run: |
          if [ "${{ github.ref_type }}" = "tag" ] || [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "build=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          BASE="${{ github.event.before }}"
          BUILD=true
          if [ -n "$BASE" ] && [ "$BASE" != "0000000000000000000000000000000000000000" ] \
              && git fetch --depth=1 origin "$BASE" 2>/dev/null; then
            # The full workspace-dependency closure of the site build: the app
            # itself, its direct workspace deps (@tilinx/agentstore-contract,
            # @tilinx/agentstore-client, @tilinx-ai/core, @tilinx-ai/store),
            # and their transitive dep @tilinx/design-tokens. A change in ANY
            # of these alters the compiled bundle, so all must gate the build —
            # ui/store was missed when the redesign made the site consume the
            # shared screens, which would have shipped app-only store changes.
            if git diff --name-only "$BASE" "${{ github.sha }}" -- \
                agentstore packages/agentstore-contract packages/agentstore-client \
                ui/core ui/store packages/design-tokens \
                .github/workflows/agentstore-image.yml \
                | grep -q .; then
              BUILD=true
            else
              BUILD=false
            fi
          fi
          echo "build=$BUILD" >> "$GITHUB_OUTPUT"

      - name: Setup pnpm
        if: steps.gate.outputs.build == 'true'
        uses: pnpm/action-setup@v4

      - name: Setup Node
        if: steps.gate.outputs.build == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install
        if: steps.gate.outputs.build == 'true'
        run: pnpm install --frozen-lockfile

      - name: Typecheck (contract)
        if: steps.gate.outputs.build == 'true'
        run: pnpm --filter @tilinx/agentstore-contract typecheck

      - name: Test (contract)
        if: steps.gate.outputs.build == 'true'
        run: pnpm --filter @tilinx/agentstore-contract test

      - name: Typecheck (agentstore)
        if: steps.gate.outputs.build == 'true'
        run: pnpm --filter tilinx-agentstore typecheck

      - name: Test (agentstore)
        if: steps.gate.outputs.build == 'true'
        run: pnpm --filter tilinx-agentstore test

      - name: Auth to GCP
        if: steps.gate.outputs.build == 'true'
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: projects/1056698080170/locations/global/workloadIdentityPools/github/providers/tilinx
          service_account: github-deploy-engine@gettilinx.iam.gserviceaccount.com

      - name: Set up gcloud
        if: steps.gate.outputs.build == 'true'
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        if: steps.gate.outputs.build == 'true'
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      - name: Build & push agentstore
        id: build
        if: steps.gate.outputs.build == 'true'
        env:
          # The Firebase web config, inlined into the client bundle at build time
          # (NEXT_PUBLIC_*). Public web identifiers by design, sourced from the
          # same FIREBASE_* repo secrets release.yml already bakes into the
          # desktop/web app — one Firebase project, one place to rotate it.
          # agents.gettilinx.ai signs users in against this project.
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
        run: |
          # Fail loudly rather than ship a bundle with a dead sign-in surface:
          # a missing NEXT_PUBLIC_FIREBASE_* value inlines as undefined and
          # silently kills publish/claim/me/admin in production.
          for var in FIREBASE_API_KEY FIREBASE_AUTH_DOMAIN FIREBASE_PROJECT_ID; do
            if [ -z "${!var}" ]; then
              echo "::error::$var is empty — set the FIREBASE_* repository secrets (shared with release.yml)" >&2
              exit 1
            fi
          done
          SHA_TAG="${{ env.REGISTRY }}/agentstore:${{ github.sha }}"
          LATEST_TAG="${{ env.REGISTRY }}/agentstore:latest"
          # Repo-root build context so the pnpm workspace (pnpm-lock.yaml +
          # sibling workspace:* packages: @tilinx/agentstore-contract,
          # @tilinx-ai/core, @tilinx/design-tokens) is reachable; the Dockerfile
          # lives in agentstore/. Mirrors engine-pod-image.yml (-f selfhost/… .).
          docker build \
            --platform linux/amd64 \
            --build-arg NEXT_PUBLIC_FIREBASE_API_KEY="$FIREBASE_API_KEY" \
            --build-arg NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="$FIREBASE_AUTH_DOMAIN" \
            --build-arg NEXT_PUBLIC_FIREBASE_PROJECT_ID="$FIREBASE_PROJECT_ID" \
            -f agentstore/Dockerfile \
            -t "$SHA_TAG" -t "$LATEST_TAG" \
            .
          docker push "$SHA_TAG"
          docker push "$LATEST_TAG"
          # Resolve the immutable digest of the pushed image for the roll below.
          DIGEST=$(gcloud artifacts docker images describe "$SHA_TAG" \
            --format='value(image_summary.digest)')
          echo "digest=$DIGEST" >> "$GITHUB_OUTPUT"

      - name: Published tags
        if: steps.gate.outputs.build == 'true'
        run: |
          echo "Published:"
          echo "  ${{ env.REGISTRY }}/agentstore:${{ github.sha }}"
          echo "  ${{ env.REGISTRY }}/agentstore:latest"
          echo "  digest ${{ steps.build.outputs.digest }}"

      # Notify cloud that a new agentstore image is ready — cloud owns the cluster
      # credentials and performs the actual roll. Payload shape matches
      # cloud/roll-engine.yml's client_payload (image, digest, sha, run_number),
      # which roll-agentstore.yml consumes there. A tag push notifies prod
      # (roll-agentstore-prod); a push to main notifies staging
      # (roll-agentstore, unchanged). A workflow_dispatch on a debug
      # branch/no ref just publishes the image, same as before.
      - name: Notify cloud repo
        if: steps.gate.outputs.build == 'true' && (github.ref_type == 'tag' || github.ref == 'refs/heads/main')
        env:
          GH_TOKEN: ${{ secrets.CLOUD_DISPATCH_TOKEN }}
        run: |
          IMAGE="${{ env.REGISTRY }}/agentstore@${{ steps.build.outputs.digest }}"
          EVENT_TYPE="roll-agentstore"
          [ "${{ github.ref_type }}" = "tag" ] && EVENT_TYPE="roll-agentstore-prod"
          gh api repos/gettilinx/cloud/dispatches \
            -f event_type="$EVENT_TYPE" \
            -F "client_payload[digest]=${{ steps.build.outputs.digest }}" \
            -F "client_payload[image]=$IMAGE" \
            -F "client_payload[sha]=${{ github.sha }}" \
            -F "client_payload[run_number]=${{ github.run_number }}"
