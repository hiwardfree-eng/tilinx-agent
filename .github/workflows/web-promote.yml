# Web promotion — release asset → production (app.gettilinx.ai)
#
# The release workflow's `build-web` job builds packages/web ONCE with the
# PRODUCTION gateway baked in and attaches it as the `web-dist.tar.gz` release
# asset. Publishing a release (draft → public, the manual QA gate) is the
# signal that the build is blessed: this workflow downloads that SAME asset and
# deploys the identical bytes to the PRODUCTION site. No rebuild — what users
# get on app.gettilinx.ai is byte-for-byte what the release built. The preview
# site is a separate axis (web-staging.yml: main-HEAD frontend baking the
# STAGING gateway) and is never touched by release bytes, so preview QA
# exercises the same frontend code but not the identical artifact.
#
# Rollback = run this workflow manually (workflow_dispatch) with an older
# release tag — the `published` event fires only once per release, so manual
# dispatch is the promotion path for anything already published. Or roll back
# in the Firebase Hosting console.
name: Web promote

on:
  release:
    types: [published]
  # Manual promote/rollback: any existing release tag that carries a
  # web-dist.tar.gz asset can be (re-)promoted to production.
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to promote (e.g. cloud-v0.5.36)"
        required: true
        type: string

# Serialize overlapping publishes so two rapid promotions can't race the same
# production site into an inconsistent state; never cancel an in-flight deploy.
concurrency:
  group: web-promote
  cancel-in-progress: false

permissions:
  contents: read # download the release asset
  id-token: write # Workload Identity Federation for the Firebase deploy

jobs:
  promote:
    # Promote published releases on either channel, plus manual dispatch:
    #   - `cloud-v*`         — the CLOUD channel, the one actually shipping. These
    #                          releases are prereleases BY POLICY (the legacy local
    #                          desktop app tracks "latest release", which cloud cuts
    #                          must not claim), so no prerelease check applies here.
    #   - `v*` non-prerelease — the legacy LOCAL channel, kept as-is for the day it
    #                          ships web bytes again.
    #   - workflow_dispatch  — manual promote/rollback by tag (the tag itself is
    #                          validated in the first step).
    #   - canonical repo     — forks have no deploy access to our GCP project.
    if: ${{ github.repository == 'gettilinx/tilinx' && (github.event_name == 'workflow_dispatch' || startsWith(github.event.release.tag_name, 'cloud-v') || (startsWith(github.event.release.tag_name, 'v') && !github.event.release.prerelease)) }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      GH_REPO: ${{ github.repository }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      TAG: ${{ github.event.release.tag_name || inputs.tag }}
    steps:
      # Manual dispatch accepts arbitrary text; only release-channel tags may
      # reach production. Fail loudly on anything else.
      - name: Validate dispatched tag
        if: ${{ github.event_name == 'workflow_dispatch' }}
        run: |
          set -euo pipefail
          case "$TAG" in
            cloud-v*|v*) echo "Promoting $TAG" ;;
            *)
              echo "::error::'$TAG' is not a release-channel tag (expected cloud-v* or v*)."
              exit 1
              ;;
          esac
      # A release published BEFORE this pipeline existed carries no web asset.
      # That is not a failure — skip gracefully with a notice rather than red-X an
      # old release the instant it is published. A PRESENT-but-empty asset, by
      # contrast, is a real problem and fails loudly below.
      - name: Check for web-dist.tar.gz asset
        id: check
        run: |
          set -euo pipefail
          if gh release view "$TAG" --json assets --jq '.assets[].name' | grep -qx 'web-dist.tar.gz'; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Release $TAG has no web-dist.tar.gz asset — nothing to promote (tag predates the web pipeline). Skipping."
          fi

      # The Firebase config (firebase.json + .firebaserc) is version-controlled,
      # not in the artifact. Check it out AT THE RELEASE TAG so production's
      # headers/target mapping match the exact commit that produced the bundle.
      - name: Checkout hosting config at the release tag
        if: ${{ steps.check.outputs.present == 'true' }}
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.release.tag_name || inputs.tag }}
          sparse-checkout: |
            packages/web/firebase.json
            packages/web/.firebaserc
          sparse-checkout-cone-mode: false

      # Download the SAME artifact build-web attached to this release and unpack
      # it into the location firebase.json's `public: "dist"` expects. Reusing the
      # asset (no rebuild) is what makes the promotion byte-identical.
      - name: Download + unpack bundle into packages/web/dist
        if: ${{ steps.check.outputs.present == 'true' }}
        run: |
          set -euo pipefail
          gh release download "$TAG" --pattern 'web-dist.tar.gz' --dir .
          if [ ! -s web-dist.tar.gz ]; then
            echo "::error::web-dist.tar.gz is present on $TAG but downloaded empty — refusing to promote a partial artifact."
            exit 1
          fi
          rm -rf packages/web/dist
          mkdir -p packages/web/dist
          tar -xzf web-dist.tar.gz -C packages/web/dist
          test -f packages/web/dist/index.html \
            || { echo "::error::unpacked bundle has no index.html — the artifact is not a web build"; exit 1; }

      - name: Auth to GCP (Workload Identity Federation)
        if: ${{ steps.check.outputs.present == 'true' }}
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: projects/1056698080170/locations/global/workloadIdentityPools/github/providers/tilinx
          service_account: github-deploy-web@gettilinx.iam.gserviceaccount.com

      # `production` maps to the tilinx-web site (see .firebaserc); firebase-tools
      # reads the ADC google-github-actions/auth wrote, so no FIREBASE_TOKEN.
      - name: Deploy to production site
        if: ${{ steps.check.outputs.present == 'true' }}
        working-directory: packages/web
        run: |
          # Pinned major: an unpinned @latest would let a breaking firebase-tools
          # release silently break promotion. Keep in sync with web-staging.yml.
          npx --yes firebase-tools@15 deploy \
            --only hosting:production \
            --project gettilinx \
            --non-interactive
          echo "::notice::Promoted $TAG to production — app.gettilinx.ai now serves the release's web-dist bytes."
